node_modules/
user-data/
auth.json
*.png
recon.json
.env
.qodo/

# Internal-only stakeholder context: names, work email addresses, a named
# client's audit situation, and internal Drive links. This repository is
# PUBLIC, so this file stays local — keep it out of any commit.
communication_context.md
